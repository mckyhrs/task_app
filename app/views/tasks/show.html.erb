<h1><%= t('.title') %></h1>

<p><%= flash[:notice] %></p>
<%= link_to t('.edit'), edit_task_path %>
<%= link_to t('.delete'), @task, method: :delete %>
<p>
	<strong><%= t('activerecord.attributes.task.task_name') %>：</strong>
	<%= @task.task_name %>
</p>
<p>
	<strong><%= t('activerecord.attributes.task.content') %>：</strong>
	<%= @task.content %>
</p>
<p>
	<strong><%= t('activerecord.attributes.task.limit_date') %>：</strong>
	<%= @task.limit_date %>
</p>
<br/>
<%= link_to t('.back'), tasks_path %>
